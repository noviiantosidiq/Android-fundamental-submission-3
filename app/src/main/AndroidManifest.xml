<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ngithubuser">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NGithubUser">

        <activity
            android:name=".ui.main.DetailActivity"
            android:label="@string/app_detail"
            android:exported="false" />
        <activity
            android:name=".ui.main.DetailFavActivity"
            android:label="@string/app_detailfav"
            android:exported="false" />
        <activity
            android:name=".ui.main.FavoriteActivity"
            android:label="@string/app_fav"
            android:exported="false" />
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_main"
            android:exported="true"
            android:theme="@style/Theme.NGithubUser.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>