<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NGithubUser" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Nprimary</item>
        <item name="colorPrimaryVariant">#333333</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/Nsecond</item>
        <item name="colorSecondaryVariant">@color/purple_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NGithubUser.splash" parent="Theme.NGithubUser">
        <item name="android:windowSplashScreenBackground">@color/Nprimary</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_logoku</item>
        <item name="android:windowSplashScreenAnimationDuration">5000</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/Nprimary</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/ic_appname</item>
        <item name="postSplashScreenTheme">@style/Theme.NGithubUser</item>
    </style>

    <style name="profileText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/Nprimary</item>
    </style>

    <style name="profileText.nama" parent="profileText">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="profileText.putih" parent="profileText">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="profileText.hitam" parent="profileText">
        <item name="android:textColor">@color/Nprimary</item>
    </style>

</resources>